/// Lists every currently `Open` offer.
pub const LIST_OPEN_OFFERS_ENDPOINT: &str = "list_open_offers";

/// Reports a single offer's full record, identified by the `OutPoint` of the
/// `SwapOutput::MakeOffer` that created it.
pub const GET_OFFER_ENDPOINT: &str = "get_offer";
